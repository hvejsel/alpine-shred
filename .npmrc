# Supply chain security: save-exact=true pins dependencies to exact versions in
# package.json (no ^ or ~ ranges). Used together with lockfile pinning and
# code-review controls as part of the supply-chain security posture.
save-exact=true
